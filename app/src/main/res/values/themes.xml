<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.JangbuRich" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/main_orange</item>
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:textColor">@color/gray_20</item>
        <item name="android:windowBackground">@color/gray_100</item>
        <item name="colorControlActivated">@color/main_orange</item> <!-- 커서 색상 -->
        <item name="android:windowLightStatusBar">true</item>   <!-- 상태바 아이콘(true: 검정 / false: 흰색) -->
        <item name="android:navigationBarColor">@color/gray_100</item> <!-- 하단 탐색바 배경 -->
    </style>

    <style name="Theme.JangbuRich" parent="Base.Theme.JangbuRich" />
</resources>